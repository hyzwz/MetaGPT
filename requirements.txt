## 一个用于异步HTTP客户端/服务器的库。
aiohttp==3.8.4
#azure_storage==0.37.0

## Django的一个扩展，用于处理WebSockets、聊天协议、物联网协议等。
channels==4.0.0
# chromadb==0.3.22
# Django==4.1.5
# docx==0.2.4

## 一个用于访问DuckDuckGo搜索引擎的库。
duckduckgo_search==2.9.4
#faiss==1.5.3

## Facebook AI Similarity Search（FAISS）的CPU版本，用于高效的相似度搜索和聚类大规模向量集。
faiss_cpu==1.7.4

## 一个用于从Python对象生成命令行接口（CLI）的库。
fire==0.4.0
# godot==0.1.1
# google_api_python_client==2.93.0

## langchain是一个用于自然语言处理的库。
langchain==0.0.231

## 一个简单强大的日志库。
loguru==0.6.0

## 一个用于访问MeiliSearch，一个开源的、快速的、相关性高的全文搜索API的库。
meilisearch==0.21.0

## 一个用于数值计算的库，提供了高效的多维数组对象和工具。
numpy==1.24.3

## OpenAI的官方Python客户端库，用于访问OpenAI的API，如GPT-3和Codex。
openai==0.27.8

## 一个用于读写Excel 2010 xlsx/xlsm/xltx/xltm文件的库。
openpyxl

## 一个强大的数据分析和操作库。
pandas==1.4.1

## 一个数据验证库，用于验证Python数据结构。
pydantic==1.10.7
#pygame==2.1.3
#pymilvus==2.2.8

## 一个成熟的全功能Python测试工具。
pytest==7.2.2

## 一个用于创建和更新Microsoft Word (.docx)文件的库。
python_docx==0.8.11

## 一个用于解析和生成YAML的库。
PyYAML==6.0
# sentence_transformers==2.2.2

## 一个用于打包Python项目的库。
setuptools==65.6.3

## 一个用于处理重试复杂和可能失败的操作的库。
tenacity==8.2.2

## 一个OpenAI的库，用于计算一个字符串在模型中使用的令牌数量。
tiktoken==0.3.3

## 一个快速、可扩展的Python进度条库。
tqdm==4.64.0
#unstructured[local-inference]
# playwright
# selenium>4
# webdriver_manager<3.9

## Anthropic的官方Python客户端库，用于访问Anthropic的API。
anthropic==0.3.6

## 一个用于检查类型注解的库。
typing-inspect==0.8.0

## 一个提供额外的类型注解支持的库。
typing_extensions==4.5.0
